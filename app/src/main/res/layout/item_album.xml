<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="album"
            type="com.mikolove.album.data.entities.AlbumView" />

        <variable
            name="clickListener"
            type="com.mikolove.album.view.AlbumClickListener" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:onClick="@{()-> clickListener.onClick(album)}">

        <TextView
            android:id="@+id/item_album_id"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="@{@string/album_title(album.albumId)}"
            app:layout_constraintBottom_toBottomOf="@+id/item_album_count"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/item_album_count" />

        <TextView
            android:id="@+id/item_album_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:text="@{@string/album_count(album.nbElement)}"
            app:layout_constraintBottom_toBottomOf="@+id/item_album_img"
            app:layout_constraintEnd_toStartOf="@+id/item_album_img"
            app:layout_constraintTop_toTopOf="@+id/item_album_img" />

        <ImageView
            android:id="@+id/item_album_img"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ic_keyboard_arrow_right_black" />

        <View
            android:id="@+id/item_album_divider"
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_marginTop="8dp"
            android:background="?android:attr/listDivider"
            app:layout_constraintEnd_toEndOf="@+id/item_album_img"
            app:layout_constraintStart_toStartOf="@+id/item_album_id"
            app:layout_constraintTop_toBottomOf="@+id/item_album_id" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>